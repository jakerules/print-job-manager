# TrueNAS SCALE docker-compose
# =============================
# For use with TrueNAS Apps → Custom App (Docker Compose).
# Paste this into the compose editor or place in the app dataset.
#
# 1. In TrueNAS: Apps → Discover → Custom App
# 2. Paste this compose config
# 3. App will be at http://<truenas-ip>:9080
#
# NOTE: TrueNAS Apps only supports pulling images (no build).
# If GHCR images are outdated, build and push from a workstation first,
# or use the TrueNAS CLI directly with docker-compose.build.yml.
#
# Optional — Google Sheets integration:
#   Mount config.ini and credentials.json via the TrueNAS storage UI,
#   then uncomment the two volume lines below and redeploy.

services:
  backend:
    image: ghcr.io/jakerules/print-job-manager/backend:9a064054e609801c658855fe92d3da1ad3de99ab
    ports:
      - "5080:5000"
    volumes:
      - ./database:/data/db
      - ./uploads:/app/uploads
      # Uncomment BOTH lines below for Google Sheets integration:
      # - ./config:/app/config:ro
      # - ./credentials.json:/app/web_app/credentials.json:ro
    environment:
      - FLASK_ENV=production
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-change-me-in-production}
      - RESET_ADMIN_PASSWORD=${RESET_ADMIN_PASSWORD:-}
      - TZ=${TZ:-America/New_York}
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5000/api/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    restart: unless-stopped

  frontend:
    image: ghcr.io/jakerules/print-job-manager/frontend:9a064054e609801c658855fe92d3da1ad3de99ab
    ports:
      - "9080:80"
    depends_on:
      - backend
    restart: unless-stopped
